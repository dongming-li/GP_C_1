<%= form_with(model: @news_posts, local: true) do |form| %>
  <% if @news_posts.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_posts.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
      <% news_posts.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :news_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :news_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
